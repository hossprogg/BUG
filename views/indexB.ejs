<h2>Search Books</h2>
<form action="/books" method="GET">
  <div>
    <label>title</label>
    <input type="text" name="title" value="<%=searchOptionss.title%>">
 </div>

  <div>
    <label>Published After<label>
    <input type="date" name="PublishedAfter" value="<%= searchOptionss.PublishedAfter %>">
  </div>

  <div>
    <label>Published Befor<label>
    <input type="date" name="PublishedBefore" value="<%= searchOptionss.PublishedBefore %>">
  </div>

  <button type="submit">Search</button>
</form>

<!-- 
<div>
  <% book.forEach(bookk => { %>
    <img height="150" width="100" src="<%= bookk.coverImagePath %>">
  <% }) %>
</div> -->

 <% book.forEach(books => { %>
  <img height="=150" width="100" src="<%= book.coverImagePath %>">
<% }) %>